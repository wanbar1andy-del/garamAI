# GARAM Environment Configuration
trading_mode: LIVE_PAPER # SHADOW | LIVE_PAPER | REPLAY | LIVE_REAL
market: KR
universe_config: config/kr_shadow_universe.yaml
risk_config: config/risk_live_paper.yaml
ai:
  enabled: true # Advisor mode (SHADOW) or Active (LIVE_PAPER/REAL)
  hard_block_threshold: 0.4 # Suppress signal if p_good < 0.4
  boost_threshold: 0.6 # Boost risk if p_good >= 0.6
